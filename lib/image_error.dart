import 'package:flutter/material.dart';

class ImageError extends StatelessWidget {
  final double fontSize;
  const ImageError({super.key, this.fontSize = 20});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.grey,
    );
  }
}
